{
  "name": "idreeswatch-app-store",
  "version": "1.0.0",
  "private": true,
  "description": "IdreesWatchOS package catalog",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node scripts/validate-source.mjs && node scripts/validate.mjs",
    "check:source": "node scripts/validate-source.mjs",
    "publish:package": "node scripts/publish-built-app.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
