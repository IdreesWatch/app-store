import watch

watch.title("Python on IdreesWatch")
watch.clear()
watch.text("This UI is driven by a real MicroPython package.")
watch.progress(35)

def advance():
    watch.text("Button event delivered safely to Python.")
    watch.progress(100)

watch.button("Run action", advance)
